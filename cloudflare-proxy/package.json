{
  "name": "xframe-cloudflare-proxy",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "check-config": "node scripts/check-config.js",
    "deploy": "node scripts/check-config.js && wrangler deploy",
    "test": "node --test"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
