! L2-4-1 — FRR configuration (generated by xForm semantic-lab-fw)
hostname L2-4-1
!
ip forwarding
ipv6 forwarding
!
interface lo
 ip address 172.16.0.25/32
!
interface eth1
!
interface eth2
!
interface eth3
!
interface eth4
!
interface eth5
 ip address 10.10.1.25/24
!
!
router bgp 65341
  no bgp ebgp-requires-policy
  no bgp default ipv4-unicast
  bgp default show-hostname
  bgp bestpath as-path multipath-relax
  bgp router-id 172.16.0.25
!
  neighbor eth1 interface remote-as 65000
  neighbor eth1 description S2-1
!
  neighbor eth2 interface remote-as 65000
  neighbor eth2 description S2-2
!
  neighbor eth3 interface remote-as 65000
  neighbor eth3 description S2-3
!
  neighbor eth4 interface remote-as 65000
  neighbor eth4 description S2-4
!
 address-family ipv4 unicast
  maximum-paths 64
  maximum-paths ibgp 64
!
  network 172.16.0.25/32
  network 10.10.1.0/24
!
  neighbor eth1 activate
!
  neighbor eth2 activate
!
  neighbor eth3 activate
!
  neighbor eth4 activate
!
 exit-address-family
 address-family ipv6 unicast
  maximum-paths 64
  maximum-paths ibgp 64
!
!
  neighbor eth1 activate
!
  neighbor eth2 activate
!
  neighbor eth3 activate
!
  neighbor eth4 activate
!
 exit-address-family
!
