# xForm browser bundle

ESM build for in-browser compile/final. Generated by `npm run bundle:browser` in the xForm package and synced here.

The shipped type and core-concept packages (`core/types/` and `core/concepts/`)
are embedded into the bundle so declarative types and compiler-owned concepts
resolve without a filesystem.
